//calcul de la moyenne

//Demander à l'utlisateur d'entrer la moyenne

/*let moyenne = Number(prompt("Entrez la moyenne de l'élève (entre 0 et 20) :"));

//boucle pour vérifier la condition 

if (moyenne >= 0 && moyenne <= 20) {

    var mention = "";
    switch (true) {
        case (moyenne >=0 && moyenne<10 ):
            mention = "insuffissant";
            break;
         case (moyenne>=10 && moyenne<12):
                mention = "Passable";
                break;
        case (moyenne >=12 && moyenne<13):
            mention = "Assez-Bien";
            break;
        case (moyenne >=13 && moyenne<14):
            mention = "Bien";
            break;
        case (moyenne >=14 && moyenne<15):
            mention = "Très bien";
            break;
            case (moyenne >=15 && moyenne<=20):
                mention = "Excelent";
                break;
        default:

        
    }

    alert("Mention de l'élève : " + mention);
} else {
    
    alert("La moyenne doit être comprise entre 0 et 20.");
}*/
